// NAMING CONVENTIION: Not a rule, so not forced to follow. But sometimes may produce erroneous code.

// 📍 Identifiers:
// Identifiers must start with a letter (A-Z or a-z), underscore (_), or dollar sign ($).
// After the first character, identifiers can include letters, digits (0-9), underscores (_), or dollar signs ($).
// Valid: myVariable, _temp, $value, var123
// Invalid: 123var (cannot start with a digit), my-var (hyphens are not allowed), class (reserved keyword).

// 📍 Package:
// If name contains multiple words then it must be sepearted by dot (.)
// example: com.sample, com.codemeals.


public class _2_Naming_conventions
{
    public static void main(String[] args)
    {

    }
}